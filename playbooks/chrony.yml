- name: jumpbox dummy
  hosts: jumpbox
  become: true

- hosts: jumpbox, ondemand, scheduler, grafana, ccportal
  become: true
  gather_facts: no
  vars_files:
    - '{{global_config_file}}'

  tasks:
  - name: Configure chrony
    include_role:
      name: chrony
