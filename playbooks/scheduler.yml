---
- hosts: scheduler
  gather_facts: no
  vars_files:
    - '{{global_config_file}}'

  tasks:
  - include_role: 
      name: pbsserver
      apply: 
        become: true
    vars:
      cc_admin: '{{admin_user}}'
      cc_password: '{{global_cc_password}}'
