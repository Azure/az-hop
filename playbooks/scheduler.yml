---
- hosts: scheduler
  gather_facts: no

  tasks:
    - include_role: 
        name: pbsserver
        apply: 
          become: true
      vars:
        pbs_admin: '{{global_admin_username}}'
        pbs_cc_headless: true
