all:
  hosts:
    localhost:
      psrp_ssh_proxy: ${jumpbox-pip}
    jumpbox:
      ansible_host: ${jumpbox-pip}
      ansible_ssh_user: ${jumpbox-user}
    scheduler:
      ansible_host: ${scheduler-ip}
      ansible_ssh_user: ${scheduler-user}
      ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p ${jumpbox-user}@${jumpbox-pip}"'
    ondemand:
      ansible_host: ${ondemand-ip}
      ansible_ssh_user: ${ondemand-user}
      ansible_ssh_common_args: '-o ProxyCommand="ssh -W %h:%p ${jumpbox-user}@${jumpbox-pip}"'
    ad:
      ansible_host: ${ad-ip}
      ansible_connection: psrp
      ansible_psrp_protocol: http
      ansible_user: hpcadmin
      ansible_password: ${ad-passwd}
      ansible_psrp_proxy: socks5h://localhost:5985
  vars:
    ad_dns: ${ad-ip} 
    ad_join_password: Microsoft12345!
    ad_join_user: domainjoinuser
    ad_join_domain: hpc.azure
    anf_home_ip: ${ anf-home-ip }
    anf_home_path: ${ anf-home-path }
    ondemand_fqdn: ${ ondemand-fqdn }
