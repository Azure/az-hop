---
batch_connect:
  template: "basic"
script:
  native:
    - "-p"
    - "<%= slot_type %>"
    - "-t"
    - "<%= '%02d:00:00' % hours %>"
    - "-N"
    - "1"
    - "--exclusive"
    - "--container-image=<%= container_image %>"
    - "--export=ALL"