#!/bin/bash
# Placeholder to add any of your custom scripts here

# If you need to write OS specific scripts then copy them in the ../files/$os_release folder and use the following lines to call it
#script_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#source "$script_dir/../files/azhop-helpers.sh" 
#read_os

#$script_dir/../files/$os_release/mystuff.sh