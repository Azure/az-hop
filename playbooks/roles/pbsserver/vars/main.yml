cyclecloud_pbspro: 2.0.21
ccportal_name: "{{cyclecloud.name | default('ccportal')}}"
cycle_webServerContextPath: "{{'/cyclecloud' if (cc_webserverpath | default('', true) | trim == '') else cc_webserverpath}}"
